#include <thread>
#include "ffClient.h"

// int main()
// {
    // std::deque<std::string> q = {};
    // std::shared_ptr<std::deque<std::string>> tmp = std::make_shared<std::deque<std::string>>(q);
    // ffGraph::ffClient client("localhost", 12345, tmp);
//
    // std::thread th([&]() {client.Start();});
//
    // while (1) {
        // std::cout << tmp->size() << "\n";
        // if (!tmp->empty()) {
            // std::cout << tmp->at(0) << "\n";
            // break;
        // }
        // sleep(5);
    // };
    // client.Stop();
    // th.join();
    // return 0;
// }


int main()
{
    return 0;
}